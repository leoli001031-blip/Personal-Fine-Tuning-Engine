# Next Pursuit Goal

Build Phase56 as evidence-span-grounded atomic extraction. Treat the Phase55 holdout and field errors as sealed historical diagnostics only. Require every non-unstated atom to include an exact supporting span from the Agent response, reject invalid or missing spans deterministically, and derive labels from validated atoms. Freeze completely new calibration and holdout splits with the same or stricter gates and zero false accepts. Do not replay runtime outputs, train, attach Hermes, or change the product default.
